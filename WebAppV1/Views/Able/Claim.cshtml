@using AppCoreV1.ABLEModels;
@using AppCoreV1.Repositories;
@using AppCoreV1.Helper;
@model PaginatedList<Claimbenefitmv>

@{
    ViewData["Title"] = @ViewBag.appName + " Claims";
    int prevpage = 0;
    int nextpage = 0;
    if (Model.HasNextPage) { nextpage = Model.PageIndex + 1; }
    if (Model.HasPreviousPage) { prevpage = Model.PageIndex - 1; }
}
<script src="~/js/ablereport.js"></script>

<div class="wrapper">
    <!-- Page Content  -->
    <div id="content">
        <div class="container-fluid">
            <div class="card">
                <div class="card-header">
                    <div class="card-title">
                        <div class="row">
                            <div style="padding-left:10px;">
                                <h4>@ViewBag.appName Claims</h4>
                                <a asp-action="Index" asp-controller="Able">Back to Home</a>
                                <a asp-action="GetClaimbenefitmvs" asp-controller="AbleDownload" asp-route-column="@ViewBag.column" asp-route-search="@ViewBag.search" asp-route-pageIndex="@Model.PageIndex" asp-route-appflag="@ViewBag.appId" class="btn btn-success">Download</a>
                            </div>
                            <div style="float:right;padding-top: 5px;padding-left:10px; width: 150px;">
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm(ViewBag.action as string, "Able", FormMethod.Get, new { @column = ViewBag.column as string, @search = ViewBag.search as string, @page = Model.PageIndex, @appId = ViewBag.appId }))
                    {
                        <partial name="_FormSearchD1" /> 
                    }
                    <div class="card-body">

                        <table id="tabldata" class="table">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">ClaimNumber</th>
                                    <!--<th scope="col">BenefitNumber</th>-->
                                    <th scope="col">PolicyNumber</th>
                                    <th scope="col">Surname</th>
                                    <th scope="col">GivenName</th>
                                    <th scope="col">DateOfBirth</th>
                                    <th scope="col">ClaimStatus</th>
                                    <th scope="col">View</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.ClaimNumber)</td>
                                        <!--<td>@Html.DisplayFor(modelItem => item.BenefitNumber)</td>-->
                                        <td>@Html.DisplayFor(modelItem => item.PolicyNumber)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Surname)</td>
                                        <td>@Html.DisplayFor(modelItem => item.GivenName)</td>
                                        <td>@Html.DisplayFor(modelItem => item.DateOfBirth)</td>
                                        <td>@Html.DisplayFor(modelItem => item.ClaimStatus)</td>
                                        <!--<td><a class="btn btn-outline-secondary" onclick="return GetClaimbenefitmv('@item.Id')"><i class="fa fa-info-circle" aria-hidden="true"></i></a></td>-->
                                        <!--<td><a class="btn btn-outline-secondary" asp-action="ClaimHub" asp-controller="AbleTabs" asp-route-search="@item.ClaimNumber" asp-route-appid="@ViewBag.appId"><i class="fa fa-info-circle"></i></a></td>-->
                                        <td><a class="btn btn-outline-secondary" asp-action="ClaimHub" asp-controller="AbleTabs" asp-route-search="@item.Id" asp-route-appid="@ViewBag.appId"><i class="bi bi-info-circle"></i></a></td>

                                    </tr>
                                }
                            </tbody>
                        </table>


                    </div>
                    <div class="card-footer">
                        <nav aria-label="...">
                            <ul class="pagination justify-content-center">
                                @if (Model.TotalPages > 1)
                                {
                                    <li id="fpLink" class="page-item"><a class="page-link" href="/Able/Claim?id=@ViewBag.id&column=@ViewBag.column&search=@ViewBag.search&column1=@ViewBag.column1&search1=@ViewBag.search1&pageIndex=1&pageSize=25&appId=@ViewBag.appId">First</a></li>
                                    @if (prevpage > 0)
                                    {
                                        <li id="ppLink" class="page-item"><a class="page-link" href="/Able/Claim?id=@ViewBag.id&column=@ViewBag.column&search=@ViewBag.search&column1=@ViewBag.column1&search1=@ViewBag.search1&pageIndex=@prevpage&pageSize=25&appId=@ViewBag.appId">Prev</a></li>
                                    }
                                    <li id="cpLink" class="page-item"><a class="page-link active" href="/Able/Claim?id=@ViewBag.id&column=@ViewBag.column&search=@ViewBag.search&column1=@ViewBag.column1&search1=@ViewBag.search1&pageIndex=@Model.PageIndex&pageSize=25&appId=@ViewBag.appId">Page @Model.PageIndex of @Model.TotalPages Pages</a></li>
                                    @if (nextpage > 0)
                                    {
                                        <li id="npLink" class="page-item"><a class="page-link" href="/Able/Claim?id=@ViewBag.id&column=@ViewBag.column&search=@ViewBag.search&column1=@ViewBag.column1&search1=@ViewBag.search1&pageIndex=@nextpage&pageSize=25&appId=@ViewBag.appId">Next</a></li>
                                    }
                                    <li id="lpLink" class="page-item"><a class="page-link" href="/Able/Claim?id=@ViewBag.id&column=@ViewBag.column&search=@ViewBag.search&column1=@ViewBag.column1&search1=@ViewBag.search1&pageIndex=@Model.TotalPages&pageSize=25&appId=@ViewBag.appId">last</a></li>
                                }
                            </ul>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<partial name="_ModalPage" />